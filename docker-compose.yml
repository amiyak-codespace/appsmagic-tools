services:
  appsmagic-tools-web:
    build:
      context: ./web
      dockerfile: Dockerfile
    container_name: appsmagic-tools-web
    restart: unless-stopped
    ports:
      - "3003:80"
    logging:
      driver: json-file
      options:
        max-size: "5m"
        max-file: "2"
